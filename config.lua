local config = {
  physicsDebug = false,
  fullscreen = true,

  borderColor = { 9 / 255, 18 / 255, 26 / 255 },
  windowWidth = 800,
  windowHeight = 600,

  levelWidth = 2100,
  levelHeight = 900,
}

return config
